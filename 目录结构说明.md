# 项目目录结构说明

## 📁 根目录文件

```
E:\meweb\
├── index.html              # 【首页】导航页，动态加载导航卡片和链接
├── _headers                # Cloudflare安全头配置（X-Frame-Options等）
├── _redirects              # Cloudflare重定向规则
├── .gitignore              # Git忽略文件配置
├── README.md               # 项目说明文档
└── 目录结构说明.md          # 本文档
```

---

## 📁 页面模块

### about/ - 个人主页
```
about/
└── index.html              # 个人主页（头像、简介、技能、项目、联系方式）
```

### blog/ - 博客
```
blog/
├── index.html              # 博客列表页（Firefly风格：左侧边栏+右侧文章卡片）
└── posts/
    └── index.html          # 文章详情页（通过 ?id=xxx 动态加载文章）
```

### drive/ - 云盘
```
drive/
└── index.html              # 云盘页面（文件卡片列表，点击跳转外部链接）
```

### guestbook/ - 留言箱
```
guestbook/
└── index.html              # 留言箱页面（毛玻璃风格，支持多级回复）
```

---

## 📁 admin/ - 管理面板

```
admin/
├── login.html              # 登录页（登录/初始化/重置密码三种模式）
├── index.html              # 管理面板主页（8个功能模块）
└── assets/
    ├── css/
    │   └── admin.css       # 管理面板样式（支持暗色模式、响应式）
    └── js/
        ├── auth.js         # 登录/初始化/重置密码逻辑
        └── dashboard.js    # 面板核心逻辑（所有功能模块）
```

### 管理面板功能模块（在 index.html 中）
- 📊 概览 - 统计卡片、快速操作、服务器状态
- 📝 文章管理 - 文章CRUD、在线编辑、文件上传、批量操作、回收站
- 👤 个人主页 - 头像、昵称、简介、技能、项目、联系方式
- ☁️ 网盘管理 - 页面设置、文件列表管理（增删改、批量操作）
- 💬 留言管理 - 留言列表、留言设置、IP黑名单、批量模式
- 🔗 导航管理 - 链接管理、卡片管理、背景设置
- 📈 访问统计 - Cloudflare Analytics 数据
- ⚙️ 系统设置 - 网站名称、修改密码、重置密钥管理

---

## 📁 nav-admin/ - 导航管理（备用）

> 已整合到管理面板，此目录可作为独立备用

```
nav-admin/
├── login.html              # 登录页（仅需密码）
├── index.html              # 导航管理主页（链接/卡片/背景三个标签页）
└── assets/
    ├── css/
    │   └── nav-admin.css   # 导航管理样式
    └── js/
        ├── nav-auth.js     # 登录逻辑
        └── nav-admin.js    # 导航管理逻辑
```

---

## 📁 assets/ - 全站共用资源

```
assets/
├── css/
│   ├── style.css           # 全局样式（暗色模式、响应式、动画）
│   └── guestbook.css       # 留言箱样式（备用，实际用内联样式）
├── js/
│   ├── main.js             # 通用脚本（导航加载、加载动画、悬浮留言按钮）
│   ├── about.js            # 个人主页逻辑
│   └── guestbook.js        # 留言箱逻辑（多级回复、旧格式转换）
└── images/                 # 图片资源（头像、favicon等）
```

---

## 📁 functions/ - Cloudflare Pages Functions（后端API）

### functions/admin/
```
functions/admin/
└── _middleware.js          # 管理面板登录验证中间件
```

### functions/nav-admin/
```
functions/nav-admin/
└── _middleware.js          # 导航管理登录验证中间件
```

### functions/api/ - API接口

```
functions/api/
├── login.js                # 管理面板登录
├── logout.js               # 登出
├── check-auth.js           # 检查登录状态
├── init.js                 # 初始化账号（首次创建）
├── reset.js                # 重置密码
├── reset-secret.js         # 重置密钥管理
├── nav-login.js            # 导航管理登录
├── nav.js                  # 导航数据（获取/保存）
├── profile.js              # 个人主页数据（获取/保存）
├── settings.js             # 系统设置（网站名称）
├── change-password.js      # 修改密码/用户名
├── analytics.js            # Cloudflare Analytics
│
├── messages.js             # 留言箱（GET列表/POST提交，支持parentId多级回复）
├── drive-settings.js       # 云盘设置（标题/描述/启用状态）
├── drive-items.js          # 云盘文件列表（CRUD+批量操作）
├── posts.js                # 博客文章（GET公开列表/POST创建）
│
├── messages/               # 留言箱子路由
│   ├── _lib.js             # 公共函数库（权限验证/KV操作）
│   ├── settings.js         # 留言设置（背景色/主题色/频率限制/背景图）
│   ├── blacklist.js        # 黑名单（获取/添加）
│   ├── [id].js             # 删除留言（递归删除子回复）
│   ├── [id]/
│   │   └── reply.js        # 站长回复
│   └── blacklist/
│       └── [ip].js         # 移除黑名单
│
├── drive-settings/         # 云盘设置子路由
│   └── verify.js           # 提取密码验证
│
├── drive-items/            # 云盘文件子路由
│   └── [id].js             # 单文件（获取/修改/删除）
│
└── posts/                  # 博客文章子路由
    ├── _lib.js             # 公共函数库
    ├── admin.js            # 管理员获取所有文章（含草稿和回收站）
    ├── batch.js            # 批量操作（移到回收站/恢复/永久删除）
    └── [id].js             # 单篇文章（获取/修改/删除）
```

---

## 📊 KV 存储数据结构

KV命名空间绑定名：**ADMIN_KV**

| Key | 说明 | 值类型 |
|-----|------|--------|
| `user:{username}` | 用户信息 | JSON |
| `system_initialized` | 系统是否已初始化 | 字符串 'true' |
| `master_password_hash` | 主密码哈希 | 字符串 |
| `session:{token}` | 管理面板session | JSON |
| `nav_session:{token}` | 导航管理session | JSON |
| `current_reset_secret` | 当前重置密钥 | 字符串 |
| `nav_data` | 导航页数据 | JSON |
| `profile_data` | 个人主页数据 | JSON |
| `guestbook_messages` | 留言箱数据 | JSON |
| `guestbook_settings` | 留言箱设置 | JSON |
| `guestbook_blacklist` | IP黑名单 | JSON |
| `site_settings` | 网站设置 | JSON |
| `drive_settings` | 云盘设置 | JSON |
| `drive_items` | 云盘文件列表 | JSON |
| `blog_posts` | 博客文章列表 | JSON |

---

## 🔧 Cloudflare 配置

### Pages 项目设置
- 构建命令：无（纯静态）
- 输出目录：`/`（根目录）
- Functions目录：`functions/`（自动识别）

### 环境变量
| 变量名 | 说明 |
|--------|------|
| `INIT_SECRET` | 初始化密钥（推荐） |
| `RESET_SECRET` | 万能重置密钥（推荐） |
| `CF_API_TOKEN` | Analytics API Token |
| `CF_ZONE_ID` | 域名Zone ID |

### R2 存储桶绑定（如使用网盘功能）
- 变量名：`BUCKET`

---

## 📝 开发说明

### 本地预览
```bash
# 安装 Wrangler（首次）
npm install -g wrangler

# 启动本地预览
wrangler pages dev . --port 8788

# 访问 http://localhost:8788
```

### 部署
```bash
# 提交到 GitHub
git add -A
git commit -m "更新说明"
git push

# Cloudflare Pages 会自动部署
```

### 注意事项
- 所有前端 fetch 必须带 `credentials: 'include'`
- 所有 cookie 的 Path 必须是 `/`
- Cloudflare Pages Functions 子路径必须用多文件独立路由
- import 路径必须仔细检查（同一目录用 `./`，上级用 `../`）
- 参数名只能包含字母、数字和下划线，不能有 `...`
