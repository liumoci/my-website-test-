# 项目目录结构说明

## 📁 根目录文件

```
E:\meweb\
├── index.html              # 【首页】导航页，动态加载导航卡片和链接
├── _headers                # Cloudflare安全头配置
├── _redirects              # Cloudflare重定向规则
├── .gitignore              # Git忽略文件配置
├── README.md               # 项目说明文档
└── 目录结构说明.md          # 本文档
```

---

## 📁 页面模块

### about/ - 个人主页
```
about/
└── index.html              # 个人主页（清新可爱风格）
```
**功能特性：**
- 顶部：圆形头像 URL + 右下角表情 + 渐变艺术字名字 + 位置/学校标签
- 左侧：小计划待办列表 + 时光进度条（今天/本周/本月/今年）
- 右侧：自我介绍（支持HTML）+ 技术栈图标 + 引用语卡片
- 底部：彩色社交按钮（QQ、邮箱、GitHub、Telegram、B站、博客、网易云、留言箱）
- 背景设置：背景图 URL、遮罩透明度、高斯模糊
- 数据来源：`GET /api/profile`
- **所有图片用 URL，不存 base64 到 KV**

### blog/ - 博客
```
blog/
├── index.html              # 博客列表页（Firefly风格三栏布局）
└── posts/
    ├── index.html          # 文章详情页（通过 ?id=xxx 动态加载）
    └── example.html        # 示例文章
```
**功能特性：**
- 三栏布局：左侧边栏（个人资料+社交链接）+ 中间文章卡片 + 右侧（时钟+最新文章）
- 文章卡片：封面图 + 标题 + 摘要 + 标签 + 日期
- 自定义背景图 URL、透明度、联系方式
- 数据来源：`GET /api/posts`、`GET /api/blog-settings`、`GET /api/profile`

### drive/ - 云盘
```
drive/
└── index.html              # 云盘页面（文件跳转模式）
```
**功能特性：**
- 文件列表（图标 + 文件名 + 描述 + 跳转链接）
- 提取密码验证
- 自定义页面标题、描述
- 数据来源：`GET /api/drive-settings`、`GET /api/drive-items`

### guestbook/ - 留言箱
```
guestbook/
└── index.html              # 留言箱页面（毛玻璃风格）
```
**功能特性：**
- 多级回复（用户可回复用户，站长可回复所有人）
- 站长回复标记
- IP 黑名单拦截
- 自定义背景色、主题色、背景图 URL
- 数据来源：`GET /api/messages`、`GET /api/messages/settings`

---

## 📁 admin/ - 管理面板

```
admin/
├── login.html              # 登录页（登录/初始化/重置密码三种模式）
├── index.html              # 管理面板主页（8个功能模块）
└── assets/
    ├── css/
    │   └── admin.css       # 管理面板样式（暗色模式、响应式）
    └── js/
        ├── auth.js         # 登录/初始化/重置密码逻辑
        └── dashboard.js    # 面板核心逻辑
```

### 管理面板功能模块

| 模块 | 说明 |
|------|------|
| 📊 概览 | 总访问量、文章数、文件数统计；快速操作按钮 |
| 📝 博客管理 | 文章 CRUD、在线编辑器、Word/TXT 上传、批量操作、回收站、博客设置（背景图URL） |
| 👤 个人主页 | 头像 URL、表情选择器、昵称、位置、学校、简介（HTML）、引用语、小计划、技能标签、背景图URL/透明度/模糊、联系方式（QQ/邮箱/GitHub/TG/B站/博客/网易云） |
| ☁️ 网盘管理 | 页面设置（标题/描述/提取密码）、文件列表管理 |
| 💬 留言管理 | 留言列表、多级回复、留言设置（背景色/主题色/背景图URL/频率限制）、IP黑名单、批量拉黑/删除 |
| 🔗 导航管理 | 链接管理、卡片管理、背景设置 |
| 📈 访问统计 | Cloudflare Analytics 数据展示 |
| ⚙️ 系统设置 | 网站名称、修改密码、重置密钥管理、一键跑路模式开关 |

### 一键跑路模式
- 在系统设置中实时开启/关闭
- 开启后除管理面板外所有页面显示"站长已跑路"+ 标准404图案
- 通过 `functions/_middleware.js` 全站中间件实现
- 状态存储在 KV 的 `site_status` key

---

## 📁 nav-admin/ - 独立导航管理（备用）

> 已整合到管理面板，此目录可作为独立备用

```
nav-admin/
├── login.html
├── index.html
└── assets/
```

---

## 📁 assets/ - 全站共用资源

```
assets/
├── css/
│   ├── style.css           # 全局样式（导航栏、暗色模式、响应式）
│   └── guestbook.css       # 留言箱样式（备用）
├── js/
│   ├── main.js             # 通用脚本（导航加载、加载动画）
│   ├── about.js            # 个人主页逻辑（备用，实际用内联脚本）
│   └── guestbook.js        # 留言箱逻辑（多级回复、设置加载）
└── images/                 # 图片资源（favicon等）
```

---

## 📁 functions/ - Cloudflare Pages Functions（后端API）

### 全站中间件
```
functions/
└── _middleware.js          # 全站中间件（一键跑路模式拦截）
```

### functions/admin/
```
functions/admin/
└── _middleware.js          # 管理面板登录验证中间件
```

### functions/nav-admin/
```
functions/nav-admin/
└── _middleware.js          # 导航管理登录验证中间件
```

### functions/api/ - API接口

```
functions/api/
├── login.js                # 管理面板登录
├── logout.js               # 登出
├── check-auth.js           # 检查登录状态
├── init.js                 # 初始化账号（首次创建）
├── reset.js                # 重置密码
├── reset-secret.js         # 重置密钥管理
├── change-password.js      # 修改密码/用户名
├── settings.js             # 系统设置（网站名称，GET公开）
├── site-status.js          # 一键跑路状态
├── analytics.js            # Cloudflare Analytics
├── profile.js              # 个人主页数据（获取/保存）
├── blog-settings.js        # 博客设置（背景/联系方式）
├── nav.js                  # 导航数据
├── nav-login.js            # 导航管理登录
│
├── messages.js             # 留言箱（GET列表/POST提交）
├── messages/               # 留言箱子路由
│   ├── _lib.js             # 公共函数库
│   ├── settings.js         # 留言设置（背景色/主题色/频率/背景图URL）
│   ├── blacklist.js        # 黑名单
│   ├── [id].js             # 删除留言
│   ├── [id]/reply.js       # 站长回复
│   └── blacklist/[ip].js   # 移除黑名单
│
├── drive-settings.js       # 云盘设置
├── drive-settings/verify.js # 提取密码验证
├── drive-items.js          # 云盘文件列表
├── drive-items/[id].js     # 单文件操作
│
├── posts.js                # 博客文章（GET公开列表/POST创建）
└── posts/                  # 博客文章子路由
    ├── _lib.js             # 公共函数库
    ├── admin.js            # 管理员获取所有文章
    ├── batch.js            # 批量操作
    └── [id].js             # 单篇文章操作
```

---

## 📊 KV 存储数据结构

KV命名空间绑定名：**ADMIN_KV**

| Key | 说明 | 值类型 |
|-----|------|--------|
| `user:{username}` | 用户信息（密码哈希等） | JSON |
| `system_initialized` | 系统是否已初始化 | 字符串 'true' |
| `session:{token}` | 管理面板session | JSON |
| `nav_session:{token}` | 导航管理session | JSON |
| `current_reset_secret` | 当前一次性重置密钥 | 字符串 |
| `site_settings` | 系统设置（网站名称） | JSON |
| `site_status` | 一键跑路模式状态 | 字符串 |
| `profile_data` | 个人主页数据 | JSON |
| `blog_settings` | 博客设置 | JSON |
| `nav_data` | 导航数据 | JSON |
| `blog_posts` | 博客文章列表 | JSON |
| `drive_settings` | 云盘设置 | JSON |
| `drive_items` | 云盘文件列表 | JSON |
| `messages` | 留言列表 | JSON |
| `message_settings` | 留言设置（含背景图URL） | JSON |
| `message_blacklist` | IP黑名单 | JSON |

### profile_data 结构
```json
{
  "avatar": "https://example.com/avatar.jpg",
  "avatarEmoji": "😊",
  "name": "昵称",
  "bio": "个人简介（支持HTML）",
  "quote": "引用语",
  "skills": ["JavaScript", "HTML"],
  "todos": [{"text": "计划内容", "done": false}],
  "backgroundImage": "https://example.com/bg.jpg",
  "backgroundOpacity": 0.5,
  "backgroundBlur": 0,
  "contact": {
    "email": "",
    "github": "",
    "qq": "",
    "telegram": "",
    "bilibili": "",
    "blog": "",
    "music": "",
    "location": "",
    "school": ""
  }
}
```

### message_settings 结构
```json
{
  "backgroundColor": "#ffffff",
  "cardColor": "#f9fafb",
  "primaryColor": "#3b82f6",
  "rateLimitMinutes": 1,
  "enabled": true,
  "backgroundImage": "",
  "backgroundImageUrl": "https://example.com/bg.jpg"
}
```

---

## 🔧 技术栈

- **前端**：原生 HTML + CSS + JavaScript（无框架）
- **后端**：Cloudflare Pages Functions
- **数据库**：Cloudflare KV
- **部署**：Cloudflare Pages
- **认证**：SHA-256 加盐哈希 + HttpOnly Cookie + Session Token
- **样式**：CSS 变量（支持暗色模式）、响应式布局

---

## ⚠️ 性能注意事项

- **图片一律用 URL**，不要上传 base64 到 KV
- KV 单个 value 建议不超过 100KB
- 头像、背景图等使用外部图床或 CDN 链接
- 大文件（如图片）不要存在 KV 中
